{
  "name": "doc-intel-frontend",
  "version": "2.0.0",
  "private": true,
  "description": "Vanilla HTML/CSS/JS frontend served as static files. No Node.js build step required.",
  "scripts": {
    "start": "python3 -m http.server 3000 --bind 0.0.0.0 --directory dist"
  }
}
